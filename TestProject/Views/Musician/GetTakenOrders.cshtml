@model IEnumerable<TestProject.Models.OrderViewModel>
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<table class="table table-sm table-striped table-bordered">
    <thead>
        <tr>
            <th>Music instrument</th>
            <th>Music description</th>
            <th>Price</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var order in Model)
        {
            <tr>
                <td>@order.MusicInstrument.Name</td>
                <td>@order.MusicDescription</td>
                <td>@order.Price</td>
                <td>
                    @using (Html.BeginForm("UploadFile", "Musician", FormMethod.Post, new { enctype = "multipart/form-data" }))
                    {
                        @Html.TextBox("PostedFile", order.PostedFile, new { type = "file", @class = "input-xlarge" })
                        @Html.Hidden("Id", order.Id);
                        @Html.Hidden("ClientId", order.ClientId);
                        @Html.Hidden("MusicInstrumentId", order.MusicInstrument.Id);
                        @Html.Hidden("MusicDescription", order.MusicDescription);
                        @Html.Hidden("Price", order.Price);
                        <input type="submit" class="btn btn-default" value="Upload file" />
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
